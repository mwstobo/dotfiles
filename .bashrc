# ls aliases
alias ls="ls --color"
alias ll="ls -lh"
alias la="ls -a"

# tmux 256 colour alias
alias tmux="tmux -2"

# sudo completion
complete -cf sudo

# ignore duplicates
export HISTCONTROL=ignoredups
